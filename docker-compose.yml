version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    environment:
      - DATABASE_PATH=/app/data/portfolio.db

  frontend:
    build: ./frontend
    ports:
      - "5173:80"
    depends_on:
      - backend
